/*
	Regions test
*/

N := 128
setgridsize(N, N, 1)
setcellsize(1e-9, 1e-9, 1e-9)
d := 1e-9 * N  

defregion(0, inverse(cylinder(d, d)))
defregion(1, cylinder(d, d))
defregion(2, cylinder(d/2, d/2))
defregion(3, transl(cylinder(d/2, d/2), d/3, 0, 0))

save(regions)
