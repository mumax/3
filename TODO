slice time stamp

slice mesh is not OK when using a part of the slice (i.e., a "slice";-). is now misused, especially in convolution. API should be more fool-proof.

fuse a standard micromag kernel for at least the local terms. uses inline functions. exchange adds to demag. extra pointers to output buffers , if not NULL, leak intermediate output.  To save memory.

use device properties for grid config

web: update parameters 
web: show/hide/auto refresh
web: graphs
web: don't fall over the edge while browser is open
web: (clean) exit button

use cuda.OnFree throughout
cuda.HostBuffer()
cuda.Recycle: check if you own it, disable contents to avoid use-after-recylce.

shift: memeset, don't clamp
