N := 400
c := 10e-9

setgridsize(N, N/2, 1)
setcellsize(c, c, c)

alpha=1
Msat = 1e6
Aex = 13e-12

setgeom(imageShape("testdata/mask.png"))
m = uniform(1, 0, 0)
setgeom(universe())

print(m.average())

expectv("m", m.average(), vector(0.118564697, 0, 0), 1e-2)
