setgridsize(512, 512, 1)

c := 4e-9
setcellsize(c, c, c)

m = uniform(1,0,0) 
randSeed(0)

ext_makegrains(40e-9, 255, 0)
print(regions.HostArray())

for i:=0; i<256; i++{
	msat.setregion(i, 1e6 + randNorm() * 1e5)
	alpha.setregion(i, 1 + randNorm() * 0.1)
}

defregion(1, circle(100*C))
defregion(100, circle(20*C))
