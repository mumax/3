bugs:
-----

use cuda.OnFree throughout: setmesh memleak
saving staggeredmask fails if not initialized first
shifting staggeredmask fails if not initialized first

improvements:
-------------

web: update parameter text boxes and image size
web: use next free port if already in use
cuda: use device properties for grid config
cuda: shift: memeset, don't clamp
cuda.HostBuffer()
cuda.Recycle: check if you own it, disable contents to avoid use-after-recylce.
LD_LIBARY_PATH ignored by build
hide buffered.Mesh(), .Comp() etc. (ZYX axes)
log.FatalErr: print line number of caller?
mask shift, set etc can LAUNCH_FAIL if mask is nil. should nicely check
move add-hoc functionality (center walls etc) to "extra" package


new features:
-------------

slice time stamp

web: (clean) exit button
web: show/hide/auto refresh
web: graphs
web: -web flag opens browser


windows:
--------
go msi installer from golang.org

git from git-scm.com
* Run Git from the windows command prompt
* Checkout as-is

mingw-w64 from mingw.org > sourceforge
* PATH=%PATH%C:\MinGW/bin


